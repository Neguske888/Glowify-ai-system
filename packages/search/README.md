# Search

This package defines provider-agnostic contracts for Glowify AI search and indexing.

It includes interfaces and metadata models for documents, queries, filters, sorting, facets, aggregations, pagination, highlights, suggestions, analyzers, tokenizers, repositories, providers, services, and registries.

The package is implementation-free and does not depend on search engines, vector databases, SDKs, persistence, or networking.
