# Orchestration

Provider-agnostic orchestration contracts for coordinating workflows, AI agents, extensions, human approvals, and distributed execution.

This package defines metadata and abstract contracts only. It does not include runtime orchestration logic, loading, persistence, networking, queues, schedulers, filesystem access, SDK integrations, or business logic.
