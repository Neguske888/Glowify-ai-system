# Events

This package defines provider-agnostic contracts for Glowify AI event bus and messaging behavior.

It contains only types, interfaces, and metadata models for publishing, subscription, dispatch, routing, retry, dead-letter handling, serialization, and middleware composition.
