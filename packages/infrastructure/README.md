# Infrastructure

This package is the platform composition root and infrastructure layer.

It provides provider-agnostic abstractions for dependency injection, bootstrap, configuration, lifecycle, logging, metrics, tracing, health, events, jobs, resilience, and utility services.

The package intentionally contains only lightweight contracts and placeholder implementations.
It does not bind the platform to any runtime provider, framework, database, cloud service, or vendor SDK.
