# Authentication & Identity

Provider-agnostic contracts for authentication, identity, sessions, credentials, verification, federation, recovery, security events, and provider metadata.

This package defines metadata models and abstract interfaces only. It does not include runtime authentication logic, persistence, networking, cookies, cryptography, middleware, SDK integrations, or provider-specific implementation.
