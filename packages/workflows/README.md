# Workflows

This package defines provider-agnostic workflow and automation contracts for Glowify AI.

It contains only interfaces and metadata models for workflow orchestration, execution, scheduling, state management, triggers, actions, conditions, retries, middleware, checkpoints, and compensation.
