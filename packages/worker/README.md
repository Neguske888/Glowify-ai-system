# Worker Runtime

Runtime worker package for Glowify.

This package contains concrete worker and job orchestration code. It is a runtime implementation package and may depend on queues, database clients, and platform services.

The package entry point is index.ts.
