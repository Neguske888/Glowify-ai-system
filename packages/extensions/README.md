# Extensions

Provider-agnostic contracts for Glowify extension, plugin, and module composition.

This package defines metadata and abstract interfaces only. It does not include runtime loading, persistence, networking, filesystem access, or SDK integrations.
