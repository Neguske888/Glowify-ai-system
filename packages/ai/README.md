# AI Runtime

Runtime AI implementation package for Glowify.

This package contains concrete actions, queues, jobs, scoring, agents, and service implementations used by the application. It is runtime code, not a contract-only package.

The package entry point is index.ts.
