# Adapters

Provider-agnostic contracts for external provider adapters, connectors, bindings, mappings, translation, serialization, health, diagnostics, versioning, compatibility, dependencies, configuration, repositories, and services.

This package defines interfaces, enums, type aliases, and metadata models only. It does not include runtime logic, SDK implementations, networking, persistence, filesystem access, or vendor-specific assumptions.
