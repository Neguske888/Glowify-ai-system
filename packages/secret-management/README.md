# Secret Management Runtime

Runtime secret management package for Glowify.

This package contains encryption, storage, audit, rotation, and secret lifecycle implementations. It is runtime code and may depend on concrete drivers or platform services.

The package entry point is index.ts.
