# Observability

This package defines provider-agnostic contracts and metadata models for logging, metrics, tracing, auditing, diagnostics, health reporting, and observability-related events.

It intentionally contains no runtime implementations, persistence, SDK initialization, or vendor-specific assumptions.
