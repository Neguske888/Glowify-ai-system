# Bootstrap

This package is the platform composition root.

It wires together platform contracts through lightweight, in-memory orchestration only. The package is intentionally provider-agnostic and does not initialize vendor SDKs, databases, authentication systems, or runtime integrations.
