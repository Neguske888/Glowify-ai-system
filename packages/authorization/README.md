# Authorization

This package defines provider-agnostic authorization and policy contracts for Glowify AI.

It covers RBAC, ABAC, policy evaluation, scopes, permissions, roles, principals, resources, access decisions, and authorization errors.

The package is implementation-free and does not depend on any authentication provider, database, SDK, or runtime behavior.
