# CQRS

This package defines provider-agnostic CQRS contracts for Glowify AI.

It contains only TypeScript interfaces and metadata models for commands, queries, buses, handlers, dispatchers, pipeline behaviors, execution contexts, validation, authorization, retry, idempotency, and result types.
