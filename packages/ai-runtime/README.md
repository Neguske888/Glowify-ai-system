# AI Runtime

Provider-agnostic contracts for AI providers, models, prompts, conversations, tool calls, routing, caching, evaluation, telemetry, and response metadata.

This package defines interfaces, enums, type aliases, metadata models, and reusable contracts only. It does not include runtime execution, provider SDKs, networking, persistence, streaming implementation, or prompt execution.
