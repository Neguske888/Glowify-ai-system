# Domain

This package contains pure domain contracts for Glowify AI.

It defines reusable interfaces and models for entities, value objects, aggregate roots, repositories, unit of work, specifications, factories, domain services, policies, commands, queries, pagination, filtering, sorting, search, validation, and domain errors.

The package is intentionally implementation-free and does not depend on persistence, runtime providers, SDKs, or infrastructure details.
