# Notifications

This package defines provider-agnostic contracts for Glowify AI notifications and communication.

It contains only interfaces, enums, metadata models, and abstractions for channels, templates, delivery, preferences, subscriptions, providers, queues, schedulers, repositories, renderers, and services.

The package is implementation-free and does not depend on SDKs, persistence, networking, or business logic.
