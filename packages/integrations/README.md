# Integrations Runtime

Runtime integrations package for Glowify.

This package contains concrete integration models and provider-specific runtime helpers. It is allowed to include implementation details and should be treated as a runtime layer, not a contract-only layer.

The package entry point is index.ts.
