# Configuration

This package defines provider-agnostic contracts for Glowify AI configuration and feature-flag behavior.

It includes interfaces and models for configuration sources, providers, repositories, stores, resolvers, validators, schemas, snapshots, versions, scopes, runtime configuration, feature flags, and evaluation results.

The package is implementation-free and does not depend on any database, SDK, or runtime provider.
